---
description: Session start — read the BiteVault and list the backlog (list only, never starts work)
---

Follow the `## /bite-start` section of `MYWORKFLOW.md` in the repo root exactly. `MYWORKFLOW.md` is the source of truth; if it is not already in context, read it first.

In short, do ONLY this, then stop:

1. Read the most recent note in the BiteVault `Sessions/` folder (`/mnt/d/Obsidian/vaults/BiteVault/Sessions/`) and summarize where we left off — decisions, current state, next steps.
2. Read `/mnt/d/Obsidian/vaults/BiteVault/Backlog.md` and list items grouped by status in this order: **In Progress → Ready → Backlog → Done**, showing `BITE-### [type] title` with its goal `G#`.
3. Read `/mnt/d/Obsidian/vaults/BiteVault/Goals.md` — list the North Star + active goals, and **flag any backlog item whose `Goal` cell is empty or `no-goal`** (alignment drift).
4. Read `/mnt/d/Obsidian/vaults/BiteVault/Progress.md` for the current app snapshot.
5. **STOP.** Do not auto-pick an item or start work. Wait for me to run `/bite-work`.
